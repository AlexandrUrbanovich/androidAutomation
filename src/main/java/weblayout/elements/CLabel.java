package weblayout.elements;

import org.openqa.selenium.WebElement;

public class CLabel extends CustomElement {

    public CLabel(WebElement wrappedElement) {
        super(wrappedElement);
    }

}
